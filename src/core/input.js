export class Input{constructor(canvas){this.keys=new Set;this.vx=0;this.vy=0;addEventListener('keydown',e=>this.keys.add(e.key.toLowerCase()));addEventListener('keyup',e=>this.keys.delete(e.key.toLowerCase()));this.canvas=canvas}vector(){let x=0,y=0;if(this.keys.has('a')||this.keys.has('arrowleft'))x--;if(this.keys.has('d')||this.keys.has('arrowright'))x++;if(this.keys.has('w')||this.keys.has('arrowup'))y--;if(this.keys.has('s')||this.keys.has('arrowdown'))y++;x+=this.vx;y+=this.vy;const m=Math.hypot(x,y)||1;return{x:x/m,y:y/m}}}
